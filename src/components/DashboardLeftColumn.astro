---
import StudyRings from './StudyRings.astro';
import StreakDisplay from './StreakDisplay.astro';

export interface Props {
  coveragePercent: number;
  consistencyPercent: number;
  masteryPercent: number;
  readinessScore: number;
  readinessLabel: 'on_track' | 'behind' | 'critical';
  streak: number;
  weekDays: { day: string; completed: boolean }[];
  entriesThisWeek: number;
  studyDaysPerWeek: number;
  upcomingExams: { label: string; date: string }[];
  syllabusPercent: number;
  user: { name?: string } | null;
}

const {
  coveragePercent, consistencyPercent, masteryPercent,
  readinessScore, readinessLabel,
  streak, weekDays, entriesThisWeek, studyDaysPerWeek,
  upcomingExams, syllabusPercent, user,
} = Astro.props;

function formatDate(dateStr: string): string {
  const d = new Date(dateStr);
  return d.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
}
---

<div class="left-column">
  <!-- Study Rings — Signature Visual -->
  <div class="ds-card-glass rings-card">
    <div class="rings-section-header">
      <span class="rings-section-label">Readiness Score</span>
    </div>
    <StudyRings
      coveragePercent={coveragePercent}
      consistencyPercent={consistencyPercent}
      masteryPercent={masteryPercent}
      readinessScore={readinessScore}
      readinessLabel={readinessLabel}
    />
  </div>

  <!-- Week Strip — Streak -->
  <div class="ds-card-glass">
    <StreakDisplay
      streak={streak}
      weekDays={weekDays}
      entriesThisWeek={entriesThisWeek}
      studyDaysPerWeek={studyDaysPerWeek}
    />
  </div>

  <!-- Upcoming Exams — Stitch style -->
  {upcomingExams.length > 0 && (
    <div class="ds-card-glass">
      <div class="section-header">
        <h3 class="section-title">Upcoming Tests</h3>
        <span class="ds-link-accent" style="font-size:var(--ds-text-xs);cursor:pointer;">View Schedule</span>
      </div>
      <div class="test-list">
        {upcomingExams.map(exam => (
          <div class="test-item">
            <div class="test-info">
              <span class="test-name">{exam.label}</span>
              <span class="test-desc">Exam</span>
            </div>
            <div class="test-date">
              <span class="test-date-value">{formatDate(exam.date)}</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  )}

  <!-- Syllabus Progress — Stitch style -->
  <div class="ds-card-glass">
    <div class="syllabus-header">
      <span class="section-title-sm">Overall Syllabus Progress</span>
      <span class="syllabus-pct">{syllabusPercent}%</span>
    </div>
    <div class="ds-progress ds-progress-lg">
      <div
        class="ds-progress-bar"
        class:list={{
          'ds-progress-bar--success': syllabusPercent >= 70,
          'ds-progress-bar--warning': syllabusPercent >= 40 && syllabusPercent < 70,
          'ds-progress-bar--danger': syllabusPercent < 40,
        }}
        style={`width:${syllabusPercent}%`}
      ></div>
    </div>
  </div>
</div>

<style>
  .left-column {
    display: flex;
    flex-direction: column;
    gap: var(--ds-space-3);
  }
  .rings-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: var(--ds-space-3);
  }
  .rings-section-header {
    width: 100%;
    margin-bottom: 0.75rem;
  }
  .rings-section-label {
    font-size: var(--ds-text-xs);
    font-weight: 500;
    color: var(--ds-text-tertiary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
  }
  .section-title {
    font-size: var(--ds-text-xs);
    font-weight: 600;
    color: var(--ds-text-primary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin: 0;
  }
  .section-title-sm {
    font-size: var(--ds-text-xs);
    color: var(--ds-text-tertiary);
    font-weight: 500;
  }
  .test-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .test-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.75rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: var(--ds-radius-lg);
    transition: background var(--ds-transition-base);
    cursor: pointer;
  }
  .test-item:hover {
    background: rgba(255, 255, 255, 0.06);
  }
  .test-info {
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
  }
  .test-name {
    font-size: var(--ds-text-sm);
    font-weight: 600;
    color: var(--ds-text-primary);
  }
  .test-desc {
    font-size: var(--ds-text-xs);
    color: var(--ds-text-tertiary);
  }
  .test-date {
    text-align: right;
  }
  .test-date-value {
    font-size: var(--ds-text-sm);
    font-weight: 600;
    color: var(--ds-accent-400);
  }
  .syllabus-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.5rem;
  }
  .syllabus-pct {
    font-size: var(--ds-text-sm);
    font-weight: 700;
    color: var(--ds-accent-400);
    font-family: var(--ds-font-mono);
  }
</style>
